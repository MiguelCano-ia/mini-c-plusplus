int main() {
    bool a = true;
    bool b = false;

    if (a && b) {
      return 1; 
    } else if (a || b) {
      return 0;
    }

    int c = 5;
}

bool equalator() {
  int a = 5;
    int b = 10;
    if (a == 5 || (b / a) > 2) {
        // Código
    }
    return true;
}